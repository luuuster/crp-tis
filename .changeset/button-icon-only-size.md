---
"@crp/design-tokens": minor
---

Botão só-ícone: ícone cresce com a caixa (icon-md 20px, icon-lg 24px) em vez de 16px fixo. Novo grupo de token `button/icon-only-size` (xs=12, sm=16, md=20, lg=24), separado do `button/icon-size` (botão com texto, que segue 16px pra alinhar com o rótulo). Espelha button.tsx (`icon`→size-5, `icon-lg`→size-6).
