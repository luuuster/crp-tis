// Anel de foco padrão para elementos não-shadcn (links, botões-ícone "crus").
// Centralizado aqui pra não duplicar entre as telas — usa o token --ring (cor da marca).
export const focusRing =
  'rounded-sm outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50'
